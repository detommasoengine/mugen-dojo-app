# Handoff — Sessione 16 Aprile 2026 (Sessione 001)

> Questo documento contiene tutto il contesto necessario per riprendere lo sviluppo di MugenDojo da qualsiasi terminale o agente AI. Leggere interamente prima di operare.
>
> **Documento di log dettagliato**: `docs/sessions/2026-04-16_session-001_initial-setup-and-devenv.md`

---

## 1. Che cos'è MugenDojo

Applicazione cross-platform (Android + WebApp) per la **gestione interna di un Dojo di Aikido**. Il sistema traccia presenze, ore di pratica, gradi (Kyu/Dan), risorse di studio, comunicazioni e progetti interni del Dojo.

**Utenti target:**
- **Aikidoka** (studenti) → usano l'app mobile
- **Caposcuola** (admin principale) → usa la dashboard web
- **Segretari** (admin secondari con permessi granulari) → usano la dashboard web

---

## 2. Documenti fondamentali — Leggere prima di codificare

| File | Contenuto | Priorità |
|------|-----------|----------|
| `CLAUDE.md` (root) | Entry point per agenti AI: stack, dominio, struttura docs | **Sempre** |
| `docs/architecture/ENGINEERING-GUIDELINES.md` | Convenzioni naming, git workflow, regole migration, sicurezza, checklist | **Sempre** |
| `docs/business/02-modello-dominio.md` | Modello entità del dominio | Per capire il dominio |
| `docs/business/03-attori-ruoli.md` | Matrice permessi, ruoli, segretari multipli | Per capire i ruoli |
| `docs/business/04-ciclo-vita-aikidoka.md` | Requisiti esame Aikikai ufficiali, ciclo di vita studente | Per logica business |
| `docs/business/05-calendario-regole.md` | Formula pesi ore, filtri grado, periodi sospensione | Per logica business |
| `docs/decisions/ADR-001-stack-tecnologico.md` | Scelta architetturale con motivazioni | Per contesto |
| `docs/project-management/ROADMAP.md` | Milestone e avanzamento | Per sapere dove siamo |
| `docs/project-management/BACKLOG.md` | Task sparsi e bug noti | Per issue aperte |

---

## 3. Regole business chiave

### Anno accademico
1 Settembre → 31 Luglio. Agosto = sospensione. Lezioni regolari: Lun/Mer/Ven 19:00–20:00.

### Formula ore pesate
```
weighted_hours = effective_hours × event_weight × role_weight
```
- `event_weight`: configurabile per evento (default 1.0, stage possono pesare diversamente)
- `role_weight`: partecipante = 1.0, conduttore (Senpai autorizzato) = `dojo.conductor_weight` (default 2.0)

### Requisiti esame (Aikikai d'Italia — personalizzabili per Dojo)
| Grado | Ore min | Mesi dal precedente | Note |
|-------|:-------:|:-------------------:|------|
| 6° Kyu | 20 | 2 | dall'iscrizione |
| 5° Kyu | 20 | 2 | |
| 4° Kyu | 60 | 3 | libretto blu al conseguimento |
| 3° Kyu | 90 | 4 | |
| 2° Kyu | 100 | 4 | |
| 1° Kyu | 120 | 5 | |
| 1° Dan | 200 | 12 | età min 15 |
| 2° Dan | 500 | 24 | |
| 3° Dan | 600 | 36 | |
| 4° Dan | 800 | 48 | |
| 5°+ Dan | — | variabile | solo nomina diretta |

### Multi-tenancy
Ogni tabella ha `dojo_id`. Le RLS policy isolano i dati per Dojo. Un utente può appartenere a più Dojo (UNIQUE su `user_id, dojo_id` in profiles).

### Permessi segretario
13 flag booleani indipendenti in `secretary_permissions`: `can_manage_users`, `can_manage_calendar`, `can_confirm_attendance`, `can_view_all_attendance`, `can_send_communications`, ecc. Tutti default `false`, abilitati dal Caposcuola.

---

## 4. Stack tecnologico

```
mugendojo/
├── apps/
│   ├── mobile/          # React Native + Expo SDK 54, Expo Router 6, RN 0.81
│   └── admin/           # Next.js 16, TailwindCSS 4, React 19
├── packages/
│   └── shared/          # @mugen/shared — tipi TS, utils, Supabase client
├── supabase/
│   ├── migrations/      # 10 file SQL (001-010), naming: YYYYMMDDNNN_descrizione.sql
│   ├── config.toml      # Config locale
│   └── seed.sql         # Dati di sviluppo (popolato in sessione 001)
├── docs/                # Documentazione versionata
├── .npmrc               # node-linker=hoisted (OBBLIGATORIO per Expo+pnpm)
├── turbo.json           # Pipeline Turborepo
├── pnpm-workspace.yaml  # Workspace declaration
└── package.json         # Script root: dev, build, lint, typecheck, db:*
```

**Dipendenze chiave:**
- pnpm 10.33.0 (package manager ufficiale)
- Turborepo (task orchestration)
- `@supabase/supabase-js` in shared, `@supabase/ssr` in admin
- Supabase CLI via `npx --yes supabase@latest` (non installabile globalmente via npm)

---

## 5. Database — Schema attuale

### 10 Migration in `supabase/migrations/` (naming: `YYYYMMDDNNN_descrizione.sql`)

| # | File | Contenuto |
|---|------|-----------|
| 001 | `20260412001_create_enums.sql` | 10 enum: user_role, grade_type, event_type, attendance_method, attendance_status, event_role, exam_outcome, resource_type, access_level, workshop_frequency |
| 002 | `20260412002_create_dojos.sql` | Entità root multi-tenant, pesi configurabili, anno accademico, trigger `update_updated_at_column()` |
| 003 | `20260412003_create_profiles.sql` | Estende auth.users con dojo_id, ruolo, grado, can_conduct. `get_user_dojo_ids()` SECURITY DEFINER. Include le policy RLS di `dojos` (spostate da 002) |
| 004 | `20260412004_create_secretary_permissions.sql` | 13 flag booleani per segretario |
| 005 | `20260412005_create_exam_requirements.sql` | Requisiti configurabili per dojo, UNIQUE(dojo_id, target_grade) |
| 006 | `20260412006_create_calendar.sql` | `lesson_templates`, `suspension_periods`, `events`, `event_grade_exceptions` |
| 007 | `20260412007_create_attendances.sql` | `attendances`, `grade_history` — partial index `WHERE status = 'registered'` |
| 008 | `20260412008_create_journals_resources.sql` | `journals`, `resources`, `glossary_entries` |
| 009 | `20260412009_create_projects_workshops.sql` | `projects`, `project_members` (con CHECK identità), `project_grade_exceptions`, `communications` |
| **010** | **`20260412010_fix_rls_helpers.sql`** | **Aggiunge `get_user_profile_ids()`, `is_head_master_of(uuid)` SECURITY DEFINER + trigger `prevent_self_escalation` + 33 policy RLS** |

> **CRITICO**: Il naming delle migration usa `YYYYMMDDNNN` (senza underscore tra data e sequenza). Non tornare al vecchio formato `YYYYMMDD_NNN` — causa duplicate key constraint in Supabase.

### RLS — Pattern standard (dopo migration 010)

Ogni policy usa **esclusivamente** le 3 funzioni SECURITY DEFINER:
```sql
-- Membership check
dojo_id IN (SELECT get_user_dojo_ids())

-- Profile ownership
profile_id IN (SELECT get_user_profile_ids())

-- Admin check
is_head_master_of(dojo_id)
```
**Zero subquery dirette su `profiles`** nelle policy. Il trigger `prevent_self_escalation` impedisce auto-modifica di role/grade/can_conduct.

### Seed data (aggiornato in sessione 001)

| Email | Password | Ruolo | Grado |
|-------|----------|-------|-------|
| `sensei@mugendojo.it` | `password123` | `head_master` | `dan_3` |
| `senpai@mugendojo.it` | `password123` | `secretary` | `kyu_1` |
| `studente@mugendojo.it` | `password123` | `aikidoka` | `kyu_4` |

Dojo: "Mugen Dojo" — id fisso: `dddddddd-dddd-dddd-dddd-000000000001`

---

## 6. Auth — Stato implementazione

### Admin (Next.js) — **COMPLETATO** ✅
| File | Ruolo |
|------|-------|
| `apps/admin/src/lib/supabase/client.ts` | Browser client (`createBrowserClient` da @supabase/ssr) |
| `apps/admin/src/lib/supabase/server.ts` | Server client con cookie handling |
| `apps/admin/src/middleware.ts` | Protezione rotte `/dashboard/*`, refresh sessione, redirect login |
| `apps/admin/src/app/login/page.tsx` | Form email/password con `signInWithPassword()` |
| `apps/admin/src/app/dashboard/page.tsx` | Dashboard protetta (verifica sessione server-side) |
| `apps/admin/src/app/auth/callback/route.ts` | Callback OAuth/email con `exchangeCodeForSession()` |
| `apps/admin/src/app/page.tsx` | Redirect immediato a `/login` |

**Testato e funzionante**: login con `sensei@mugendojo.it` / `password123` → redirect a `/dashboard`. ✅

### Mobile (Expo) — **PARZIALE** ⚠️
| File | Ruolo | Stato |
|------|-------|-------|
| `apps/mobile/lib/supabase.ts` | Client Supabase con AsyncStorage, typed con `Database` | ✅ Presente |
| Login page | Form email/password | ❌ Non ancora creata |

---

## 7. Variabili d'ambiente

> **ATTENZIONE**: Le chiavi locali Supabase cambiano ad ogni `db:start`. Dopo `db:stop` + `db:start`, verificare con `pnpm run db:status` e aggiornare i file `.env`.

**`apps/admin/.env.local`** (non committare):
```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<da pnpm db:status>
```

**`apps/mobile/.env.local`** (non committare):
```
EXPO_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
EXPO_PUBLIC_SUPABASE_ANON_KEY=<da pnpm db:status>
```

**Ultimo valore noto** (da sessione 001, potrebbe essere cambiato):
```
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_ACJWlzQHlZjBrEguHvfOxg_3BJgxAaH
```

---

## 8. Script disponibili

| Script | Comando | Note |
|--------|---------|------|
| Dev completo | `pnpm dev` | Avvia mobile + admin in parallelo |
| Dev solo admin | `pnpm dev:admin` | Next.js dev server |
| Dev solo mobile | `pnpm dev:mobile` | Expo dev server (LAN) |
| Dev mobile tunnel | `pnpm dev:mobile:tunnel` | Expo via ngrok (WAN) |
| DB start | `pnpm db:start` | Avvia container Docker Supabase |
| DB stop | `pnpm db:stop` | Ferma container |
| DB reset | `pnpm db:reset` | Reset completo + applica seed |
| DB migrate | `pnpm db:migrate` | Applica migration pendenti |
| DB types | `pnpm db:types` | Rigenera `packages/shared/src/types/database.types.ts` |
| DB status | `pnpm db:status` | Mostra URL e chiavi correnti |

---

## 9. Stato stack al termine della sessione 001

| Servizio | URL | Stato |
|---|---|---|
| Supabase API | `http://127.0.0.1:54321` | ✅ Funzionante (da riavviare al prossimo dev) |
| PostgreSQL | `postgresql://postgres:postgres@127.0.0.1:54322/postgres` | ✅ |
| Supabase Studio | `http://127.0.0.1:54323` | ✅ |
| Mailpit | `http://127.0.0.1:54324` | ✅ |
| App Admin | `http://localhost:3000` | ✅ Login + dashboard funzionanti |
| App Mobile | `exp://192.168.1.106:8081` | ⚠️ Web OK, Expo Go Android: issue rete aperta |

---

## 10. Problemi noti — risolti e aperti

### Risolti in sessione 001
| Problema | Soluzione |
|----------|-----------|
| Duplicate key migration (versione estratta da filename) | Rinominati file: `20260412_NNN_` → `20260412NNN_` |
| Dipendenza circolare RLS `dojos` → `profiles` | Policy `dojos` spostate in migration 003 |
| 7 bug nelle migration (policy, index, constraint) | Fix in `003`, `006`, `007`, `009`, `010` |
| GoTrue 500 su login (NULL nelle colonne varchar di `auth.users`) | Seed corretto: `''` per varchar, `NULL` per phone |
| `lightningcss` binary mancante | Symlink + script `postinstall` in `package.json` root |
| `next.config.ts` opzione deprecata | `experimental.serverComponentsExternalPackages` → `serverExternalPackages` (top-level) |
| `dev` script mancante in `apps/mobile` | Aggiunto `"dev": "expo start"` |
| Metro partiva dalla root del monorepo | Creato `apps/mobile/metro.config.js` con `watchFolders` e `nodeModulesPaths` |
| Homepage admin mostrava default Next.js | `apps/admin/src/app/page.tsx` → `redirect('/login')` |
| `pnpm-workspace.yaml` campo non valido | (da fare nella prossima sessione — non causa errori) |

### Aperti — priorità
| ID | Priorità | Descrizione |
|----|----------|-------------|
| P1 | Alta | Expo Go Android: `Failed to download remote update` — probabilmente rete diversa o QR scaduto. Usare `--tunnel` o verificare stessa rete LAN |
| P2 | Media | `@react-native-async-storage/async-storage@3.0.2` incompatibile con Expo 54 (attesa: `2.2.0`) |
| P3 | Bassa | Symlink `lightningcss` non portabile su CI — alternativa: dipendenza esplicita in `apps/admin/package.json` |
| P4 | Media | Chiavi Supabase cambiano ad ogni `db:start` — fix: configurare JWT secret statico in `supabase/config.toml` |
| P5 | Alta | Login page mobile da creare — **prossimo task obbligatorio** |
| P6 | Bassa | `pnpm-workspace.yaml` contiene `nodeLinker: hoisted` (non valido — solo `.npmrc`) |

---

## 11. Prossimi passi — Sessione 002

### Obiettivo: completare Milestone 2

**Obbligatori:**
1. [ ] Creare `apps/mobile/app/login.tsx` — schermata login con email/password + `signInWithPassword()`
2. [ ] Creare struttura tab principale mobile: `(tabs)/index.tsx` (Presenze), `(tabs)/calendar.tsx`, `(tabs)/profile.tsx`, `(tabs)/journal.tsx`
3. [ ] Rigenerare tipi TypeScript con dati reali: `pnpm run db:types` → sovrascrive stub in `packages/shared/src/types/database.types.ts`
4. [ ] Testare connessione Expo Go Android (stessa rete o `--tunnel`)

**Raccomandati:**
5. [ ] Fix `@react-native-async-storage/async-storage` → versione `2.2.0`
6. [ ] Rimuovere `nodeLinker: hoisted` da `pnpm-workspace.yaml` (è in `.npmrc`, dove serve)
7. [ ] Aggiornare `docs/business/02-modello-dominio.md` con schema DB finale (da migration 010)
8. [ ] Aggiornare `docs/project-management/ROADMAP.md` — M2 parzialmente completata

---

## 12. Comandi utili avvio sessione

```bash
# 1. Verificare stato DB (se i container sono già in esecuzione)
pnpm run db:status

# 2. Se i container non sono attivi, riavviare
pnpm run db:start

# 3. Aggiornare .env.local con le nuove chiavi (se cambiate)
# Copiare ANON KEY da db:status in:
#   apps/admin/.env.local → NEXT_PUBLIC_SUPABASE_ANON_KEY
#   apps/mobile/.env.local → EXPO_PUBLIC_SUPABASE_ANON_KEY

# 4. Rigenerare tipi TypeScript
pnpm run db:types

# 5. Avviare solo Admin per verificare che funzioni
pnpm run dev:admin

# 6. Avviare Mobile
pnpm run dev:mobile

# Kill porta 3000 occupata da Next.js zombie
lsof -ti :3000 | xargs -r kill -9

# Kill porta 8081 occupata da Metro zombie
lsof -ti :8081 | xargs -r kill -9

# Accesso diretto al DB
docker exec -it supabase_db_MugenDojo psql -U postgres
```

---

## 13. Convenzioni critiche

- **Lingua**: UI in italiano, codice/commenti in inglese
- **Commit**: Conventional Commits — `type(scope): description`. Scopes: `mobile`, `admin`, `shared`, `supabase`, `docs`, `root`
- **DB naming**: snake_case inglese (tabelle, colonne, policy)
- **Migration naming**: `YYYYMMDDNNN_descrizione.sql` — **senza** underscore tra data e sequenza. Mai modificare migration già applicate.
- **RLS**: sempre attivo su ogni tabella. Usare i 3 helper SECURITY DEFINER, mai subquery dirette su `profiles`
- **Commit progressivi**: ogni blocco logico ha il suo commit, vietati commit monolitici
- **Package manager**: pnpm (non npm, non yarn)
- **Segreti**: mai in git — `.env.local` per le chiavi locali

---

## 14. Ambiente di sviluppo

| Componente | Versione | Note |
|------------|----------|------|
| OS | Windows 10 Pro | Drive NTFS montato in WSL2 come `/mnt/3C68BC2468BBDB3A/...` |
| Node.js | 22.20.0 | |
| pnpm | 10.33.0 | globale |
| Docker Desktop | 28.0.4 | Necessario per Supabase locale. Path NTFS aggiunto a File Sharing |
| Supabase CLI | 2.89.1 | via npx, non globale |
| Git | presente | branch principale: `main` |
| IDE | VSCode | con estensioni TypeScript |

**Nota NTFS**: Il progetto è su un drive NTFS. Per il corretto funzionamento di Docker:
1. Docker Desktop → Settings → Resources → File Sharing: aggiungere `/mnt/3C68BC2468BBDB3A`
2. Git safe.directory: `git config --global --add safe.directory /mnt/3C68BC2468BBDB3A/Laboratorio/Informatica/Progetti/AIKIDO/MugenDojo`

---

*Generato il 16 aprile 2026 — fine Sessione 001 (Setup iniziale e ambiente di sviluppo).*
